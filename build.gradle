plugins {
    id 'com.android.application' version '8.7.0' apply false
}

allprojects {
    gradle.projectsEvaluated {
        tasks.withType(com.android.build.gradle.tasks.PackageApplication) {
            doFirst {
                delete android.signingConfigs.debug.storeFile
            }
        }
    }
}
